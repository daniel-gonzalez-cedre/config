yabai -m config layout bsp
# yabai -m config layout stack
# yabai -m config layout float

# yabai -m config window_placement second_child

# padding
yabai -m config left_padding 3
yabai -m config right_padding 3
yabai -m config top_padding 3
yabai -m config bottom_padding 3
yabai -m config window_gap 3

yaba -m config mouse_follows_focus on

yaba -m config mouse_modifier ctrl
yaba -m config mouse_drop_action stack  # dropping one window on another

# yaba -m config mouse_drop_action swap  # dropping one window on another
# yaba -m config mouse_action1 move  # <left click> + drag
# yaba -m config mouse_action2 resize  # <right click> + drag

# yabai -m rule --add app!="^(Safari|Firefox|Librewolf|Floorp|Terminal|Alacritty|Wezterm|Slack|Calendar)$" manage=off

yabai -m rule --add app="^Zoom$" manage=off
yabai -m rule --add app="^Messages$" manage=off
yabai -m rule --add app="^Passwords$" manage=off
yabai -m rule --add app="^System Settings$" manage=off
yabai -m rule --add app="^Calculator$" manage=off
yabai -m rule --add app="^Hammerspoon$" manage=off
