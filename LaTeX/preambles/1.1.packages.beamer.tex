\documentclass[final]{beamer}  % SET DOCUMENT CLASS
\ifposter
  \usepackage{etoolbox}
  \AtBeginEnvironment{microtype}{\let\MT_delim_set_star\relax}
  \usepackage[size=a0, orientation=landscape]{beamerposter}
  % \setbeamersize{text margin left=2cm, text margin right=2cm}
\fi

% MATH
\usepackage{amsmath, amsfonts, amssymb, amstext, amscd, amsthm}
\usepackage{bm, faktor, mathrsfs, mathtools, pict2e, thmtools, xfrac}

% FONTS
\usepackage{bbm, CJKutf8, dsfont, marvosym, stmaryrd}
\usepackage[sb]{ebgaramond}
% \usepackage{garamondlibre}
\usepackage[absolute,overlay]{textpos}

% COLORS
\usepackage{color, soul, xcolor}
\usepackage[\MintedOption]{tcolorbox}
\tcbuselibrary{hooks}
\ifminted
  \usepackage[outputdir=/tmp/latexmk]{minted}
  \usepackage{pmboxdraw}
  \let\textblockpm\textblock
  \let\textblock\relax
\else
\fi

% FORMATTING
\usepackage{listings, parskip, verbatim, upquote}
\usepackage[normalem]{ulem}
\usepackage[framemethod=TikZ]{mdframed}
\usepackage[group-separator={,},group-minimum-digits={3}]{siunitx}

% TABLES
\usepackage{booktabs, colortbl, makecell, multirow}

% FIGURES
\usepackage{float}

% HEADERS / FOOTERS
\usepackage{fancyhdr, lastpage, perpage}

% MISCELLANEOUS
\usepackage{epigraph, lipsum}
\usepackage[nodayofweek]{datetime}

% PROGRAMMING
\usepackage{chngcntr, etoolbox, ifthen, pgffor, xargs, xfp}

% REFERENCES
\usepackage{hyperref, url}
\usepackage[nameinlink]{cleveref}

% TIKZ
\usepackage{tikz, tikz-3dplot, pgfplots}
\usepackage{pgf-pie}
% \usepackage{pgfgantt}
