\documentclass[nobib,justified,\DraftOption]{tufte-handout}  % SET DOCUMENT CLASS

% MATH
\usepackage{amsmath, amsfonts, amssymb, amstext, amscd, amsthm}
\usepackage{bm, faktor, mathrsfs, mathtools, pict2e, thmtools, xfrac}

% FONTS
\usepackage{bbm, CJKutf8, dsfont, fontawesome, marvosym, microtype, stmaryrd, tipa}

% FORMATTING
\usepackage{arydshln, fancyvrb, listings, parskip, verbatim, upquote}
\usepackage[normalem]{ulem}
\usepackage[shortlabels]{enumitem}
\usepackage[framemethod=TikZ]{mdframed}
\usepackage[group-separator={,},group-minimum-digits={3}]{siunitx}

% COLORS
\usepackage{color, soul, xcolor}
\usepackage[\MintedOption]{tcolorbox}
\tcbuselibrary{hooks}
\ifminted
  \usepackage{minted}
  \usepackage{fontspec}
  % SATUS BUG FIX [tufte+fontspec LuaLaTex]
  \renewcommand\allcapsspacing[1]{{\addfontfeature{LetterSpace=15}#1}}
  \renewcommand\smallcapsspacing[1]{{\addfontfeature{LetterSpace=10}#1}}
  % FINEM BUG FIX [tufte+fontspec LuaLaTex]
  \setmonofont{BerkeleyMono}[
    UprightFont = *-Regular,
    BoldFont = *-Bold,
    ItalicFont = *-Italic,
    BoldItalicFont = *-BoldItalic,
    Extension = .otf,
    Scale=MatchLowercase,
  ]
  % \setmonofont{CommitMono-450-01111-00000011000-450}[
    % UprightFont = *-Regular,
    % ItalicFont = *-Italic,
    % Extension = .otf,
    % Scale=MatchLowercase,
  % ]
\else
\fi

% TABLES
\usepackage{booktabs, colortbl, makecell, multirow}
\usepackage[math]{cellspace}  % better cell spacing for tables

% FIGURES
\usepackage{float, graphicx}

% HEADERS / FOOTERS
\usepackage{fancyhdr, lastpage, perpage}

% MISCELLANEOUS
\usepackage{epigraph, lipsum}
\usepackage[nodayofweek]{datetime}

% PROGRAMMING
\usepackage{chngcntr, etoolbox, ifthen, imakeidx, pgffor, xargs, xfp}

% REFERENCES
\usepackage{hyperref, url}
\usepackage[nameinlink]{cleveref}

% TIKZ
\usepackage{tikz, tikz-3dplot, pgfplots}
\usepackage{pgf-pie}
% \usepackage{pgfgantt}
